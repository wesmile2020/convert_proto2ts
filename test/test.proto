syntax /* syntax declaration */ = "proto2";

package /* package declaration */ test_package.v1;

import public "google/protobuf/descriptor.proto" /* 导入公共描述符 */;

option java_package = "com.example.generated" /* Java 包名 */;

enum /* enum declaration*/ Gender0_1 {
  // 必须从 0 开始
  MOBILE = 0;        // 默认值
  HOME = 1 /* 家庭 */;
  WORK = 2;
  
  // 可以使用别名
  
  // extensions 100 to max;

  // 预留值（防止其他开发者使用这些编号）
  reserved 3, 10 to 19;
  reserved "FAX", "PAGER" /* 保留字段名 */;  // 预留名称
}

message interface {
  required string name = 1 [json_name = "full_name",];
  required int32 age = 2 [json_name = "age"];
  repeated string tags = 3;

  // message address {
  //   required string street = 1;
  //   required string city = 2;
  // }

  // message BaseMessage {
  //   required int32 id = 1;
  //   optional string description = 2;
  //   extensions 3, 10 to 199;
  //   reserved  "msg";
  //   reserved 4, /* end*/ 9;
  // }

  // extend BaseMessage {
  //   optional string extra_field = 10;
  // }
}

